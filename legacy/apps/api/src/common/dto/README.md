# DTOs

Request/response DTOs for every endpoint land here (README §11, README §35 sample payloads). Zod source-of-truth schemas live in `packages/shared-types` (FE+BE shared); NestJS pipes + OpenAPI decorators are applied in Doc 3.
