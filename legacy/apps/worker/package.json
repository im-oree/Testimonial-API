{
  "name": "@testimonial-api/worker",
  "version": "0.1.0",
  "private": true,
  "description": "Async job processor (separate deploy). Consumes BullMQ queues published by apps/api: social import, webhook delivery, stats aggregation, email digests, quota resets. Doc 2 implements processor logic.",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/main.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint \"src/**/*.ts\"",
    "verify:doc1": "tsx ../../scripts/check-doc1.ts"
  },
  "dependencies": {
    "@testimonial-api/domain": "*",
    "bullmq": "^5.34.0",
    "ioredis": "^5.4.1"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "typescript": "^5.6.3"
  }
}
