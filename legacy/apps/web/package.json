{
  "name": "@testimonial-api/web",
  "version": "0.1.0",
  "private": true,
  "description": "Testimonial API — single website (tenant workspace + platform console + public forms) per 2026-09-08 consolidation.",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests",
    "lint": "eslint --no-error-on-unmatched-pattern \"app/**/*.{ts,tsx}\" \"hooks/**/*.ts\" \"lib/**/*.ts\" \"stores/**/*.ts\""
  },
  "dependencies": {
    "@testimonial-api/shared-types": "*",
    "@testimonial-api/ui": "*",
    "@tanstack/react-query": "^5.66.0",
    "next": "^15.1.6",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "zustand": "^5.0.3"
  }
}
