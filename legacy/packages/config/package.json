{
  "name": "@testimonial-api/config",
  "version": "0.1.0",
  "private": true,
  "description": "Shared tooling configs for the monorepo (eslint base, tsconfig presets, tailwind preset).",
  "files": ["eslint", "tsconfig", "tailwind"]
}
